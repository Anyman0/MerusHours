<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:model="clr-namespace:MerusHours.Models"
             x:Class="MerusHours.Views.ModifyHourPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Work Name" FontSize="Medium"></Label>
            <Entry x:Name="WorkEntry" FontSize="Medium"></Entry>
            <Label Text="Project Name" FontSize="Medium"></Label>
            <Entry x:Name="ProjectEntry" FontSize="Medium"></Entry>
            <Label Text="Activity Name" FontSize="Medium"></Label>
            <Entry x:Name="ActivityEntry" FontSize="Medium"></Entry>
            <Label Text="Hours" FontSize="Medium"></Label>
            <Picker x:Name="HoursPicker" FontSize="Medium"></Picker>
            <Label Text="Date" FontSize="Medium"></Label>
            <DatePicker x:Name="DatePicker" Format="dddd : dd.MM.yyyy" FontSize="Medium"></DatePicker>
            <StackLayout Orientation="Horizontal">
                <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Save" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>